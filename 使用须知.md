# 使用须知与环境准备

本文档合并了旧版环境搭建、启动须知和 Web 运行说明，作为日常使用入口。专项测试细节请看 Hadoop、CloudStack、VM、KVM 和 Web 控制器各自的 README。

## 1. 推荐实验拓扑

```text
Mac / 物理机
└── Ubuntu 24.04 实验宿主机
    ├── Web 控制器：web_controller
    ├── VM/KVM 注入器：vm_injection
    ├── Hadoop/CloudStack/KVM 模块：kvm_injection
    ├── Kubernetes/k3s + Chaos Mesh
    └── QEMU/KVM 三节点虚拟机
        ├── master  -> 127.0.0.1:2220
        ├── slave1  -> 127.0.0.1:2221
        └── slave2  -> 127.0.0.1:2222
```

当前代码默认使用英文目录：

| 旧叫法 | 当前路径 |
| --- | --- |
| `虚拟机注入` | `vm_injection` |
| `kvm注入` | `kvm_injection` |
| Web 前端 | `web_controller/static` |

## 2. 系统依赖

Ubuntu 宿主机建议安装：

```bash
sudo apt update
sudo apt install -y build-essential gcc make \
  linux-headers-$(uname -r) \
  iproute2 iptables curl wget openssh-client \
  python3 python3-venv
```

如需 KVM、QEMU、Chaos Mesh：

```bash
sudo apt install -y qemu-system-aarch64 qemu-utils libvirt-daemon-system
kubectl version --client
```

Chaos Mesh 本身需要已安装在 Kubernetes/k3s 集群中，并能查询这些 CRD：

```bash
kubectl get podchaos,networkchaos,stresschaos -A
```

## 3. 编译

```bash
cd /Users/venele/Downloads/fault-injection

cd vm_injection
make all

cd ../kvm_injection
make all
```

编译后常用二进制：

| 二进制 | 来源 | 作用 |
| --- | --- | --- |
| `vm_injection/process_injector` | `process_injector.c` | 进程 crash/hang/resume |
| `vm_injection/network_injector` | `network_injector.c` | delay/loss/partition/corrupt/clear |
| `vm_injection/cpu_injector` | `cpu_injector.c` | CPU 压力 |
| `vm_injection/mem_leak` | `memleak_injector.c` | 内存占用 |
| `vm_injection/mem_injector` | `mem_injector.c` | ptrace 内存篡改 |
| `vm_injection/reg_injector` | `reg_injector.c` | ARM64 寄存器注入 |
| `vm_injection/kvm_injector` | `kvm_injector.c` | KVM 用户态注入入口 |
| `kvm_injection/hadoop-fi/hadoop_injector` | `hadoop_injector.c` | Hadoop 场景注入 |
| `kvm_injection/cloudstack-fi/cloudstack_injector` | `cloudstack_injector.c` | CloudStack 场景注入 |
| `kvm_injection/cluster_controller` | `cluster_controller.c` | CLI 统一菜单 |

## 4. Web 配置

编辑 `web_controller/config.json`：

```json
{
  "ssh": {
    "user": "root",
    "identity_file": "/home/venele/.ssh/fi_vm"
  },
  "nodes": [
    {"name": "master", "host": "127.0.0.1", "port": 2220, "role": "master", "local": false},
    {"name": "slave1", "host": "127.0.0.1", "port": 2221, "role": "slave", "local": false},
    {"name": "slave2", "host": "127.0.0.1", "port": 2222, "role": "slave", "local": false}
  ],
  "kubernetes": {
    "kubectl": "ssh -o BatchMode=yes -o StrictHostKeyChecking=no -o ConnectTimeout=5 -p 2220 ubuntu@127.0.0.1 sudo k3s kubectl",
    "default_namespace": "default",
    "chaos_namespace": "chaos-mesh"
  }
}
```

重点检查：

- `hadoop.injector` 必须是 master 节点上可执行的路径，当前示例为 `/root/hadoop-fi/hadoop_injector`。
- `vm.*` 和 `kvm.injector` 是控制器所在宿主机上的路径。
- `kubernetes.kubectl` 可以是本地 `kubectl`，也可以是 SSH 包装命令。
- 需要 root 的动作会使用 `sudo -n`，必须提前配置免密 sudo。

## 5. 启动

推荐：

```bash
cd /Users/venele/Downloads/fault-injection
python3 -m venv .venv
. .venv/bin/activate
pip install -r web_controller/requirements.txt
./start_frontend.sh
```

手动启动：

```bash
uvicorn web_controller.app:app --host 0.0.0.0 --port 8080
```

浏览器访问 `http://<宿主机IP>:8080`。

## 6. Chaos Mesh 快速验证

```bash
# 查看集群和 Chaos 资源
kubectl get nodes -o wide
kubectl get podchaos,networkchaos,stresschaos -A

# 部署演示应用
kubectl create deployment nginx-demo -n default --image=nginx --replicas=2 --dry-run=client -o yaml | kubectl apply -f -

# Web 中可执行：Pod Kill、Container Kill、网络延迟、网络丢包、CPU 压力、内存压力
```

清理：

```bash
kubectl delete podchaos,networkchaos,stresschaos --all -n default --ignore-not-found
kubectl delete deployment/nginx-demo -n default --ignore-not-found
```

## 7. Hadoop 启动摘要

```bash
# 宿主机启动三台 QEMU 节点
cd vm_injection
./run_cluster.sh master
./run_cluster.sh slave1
./run_cluster.sh slave2

# master 内启动 Hadoop
source /etc/profile
hdfs --daemon start namenode
hdfs --daemon start secondarynamenode
yarn --daemon start resourcemanager
ssh slave1 "source /etc/profile; hdfs --daemon start datanode; yarn --daemon start nodemanager"
ssh slave2 "source /etc/profile; hdfs --daemon start datanode; yarn --daemon start nodemanager"
jps
```

更完整的恢复流程见 `Hadoop 集群环境恢复与启动手册.md`。

## 8. 常用清理命令

```bash
# Chaos Mesh
kubectl delete podchaos,networkchaos,stresschaos --all -n default --ignore-not-found

# Hadoop
sudo ./kvm_injection/hadoop-fi/hadoop_injector delay-clear
sudo ./kvm_injection/hadoop-fi/hadoop_injector loss-clear
sudo ./kvm_injection/hadoop-fi/hadoop_injector reorder-clear
sudo ./kvm_injection/hadoop-fi/hadoop_injector isolate-clear
sudo ./kvm_injection/hadoop-fi/hadoop_injector mem-stress-clear
sudo ./kvm_injection/hadoop-fi/hadoop_injector disk-fill-clear

# VM
sudo ./vm_injection/network_injector 0

# KVM
sudo ./vm_injection/kvm_injector clear
```

## 9. 风险提示

- 内核模块、KVM 注入、iptables、tc、cgroup、磁盘填充都可能影响宿主机稳定性。
- 不要在生产环境运行。
- 注入前保存虚拟机快照或准备可重建镜像。
- 执行网络隔离和磁盘填充后必须清理。
